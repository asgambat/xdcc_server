# Audit Findings — xdcc_server

Generated by `/improve` on 2026-06-25 against commit `d42d1e7` (develop branch).

Sources: 4 parallel Explore subagents (security+deps, perf+tech-debt, tests+DX, docs+direction); pre-existing concurrency analysis at `docs/ANALISI_BACKEND_BUG_CONCORRENZA.md` (used as starting signal for concurrency/bug findings, then spot-verified).

Verification: highest-leverage items confirmed against source. Lower-confidence items flagged.

---

## Findings table (ordered by leverage)

| # | Finding | Category | Impact | Effort | Risk | Confidence | Evidence |
|---|---------|----------|--------|--------|------|------------|----------|
| 1 | **Concurrency: `queue.Manager.tryDispatch` releases `dispatchMu` before `startDownload` reserves the slot atomically** — two concurrent dispatches can both see the channel as free and launch two downloads for the same bot/canale | bug | Two parallel downloads for same channel, violates "1 active per channel" invariant; can corrupt files, hit DCC rate limits | M | MED | HIGH | `internal/queue/manager.go:527-621,623-864` (see also doc §2.1) |
| 2 | **`config.Config` is mutated in place by `Save`/`ApplyPartial` while many goroutines read it — no `sync.RWMutex`** | bug | Data race detected by `go test -race`; inconsistent reads in `ircmanager`, `api`, `queue` | M | MED | HIGH | `internal/config/config.go:357-384,907-1134`; see doc §2.26 |
| 3 | **`channellog.Logger.Close` not idempotent** — concurrent `Close()` calls can `panic: close of closed channel` | bug | Process panic during shutdown if multiple components call `Close` | S | LOW | HIGH | `internal/channellog/channellog.go:278-307`; see doc §2.14 |
| 4 | **`api.Store` is concrete `*store.SQLiteStore`, not focused interfaces** — violates CLAUDE.md layering rule | tech-debt | Cannot mock handlers in tests; couples API to SQLite; blocks testability | M | MED | HIGH | `internal/api/api.go:35` (verified) |
| 5 | **Zero test coverage in `internal/notifier`, `internal/downloader`, `internal/sse`** + **zero frontend tests** | tests | Silent breakage of webhook/email delivery, DCC transfer orchestration, SSE fan-out, and any Svelte UI change | M | MED | HIGH | `find internal/{notifier,downloader,sse} -name '*_test*'` returns empty; `find web/src -name '*test*'` empty (verified) |
| 6 | **Missing SQLite indexes for `downloads` query patterns** — `GetDownloadHistory` filters on `filename LIKE`, `date(completed_at) >=`, `FilenamesExist` uses `LOWER(filename)`; only indexes are on `status`, `channel`, `bot+server`, `status+channel` | perf | Full table scan on history/watchlist/dedup queries; degrades linearly with row count | S | LOW | HIGH | `internal/store/sqlite.go:567-714` filters vs `internal/store/schema.go:113-115,174` indexes (verified) |
| 7 | **`/api/events` SSE endpoint has no auth** — `router.go:123` registers it before the `RequireAdminToken` group at line 131 | security | Anyone reading the SSE stream sees download progress, filenames, server status, log entries (incl. private IRC msgs if `log_private_messages` enabled) | S | LOW | HIGH | `internal/api/router.go:123,131` (verified) |
| 8 | **`Taskfile.yml` `build:all` deps are commented out** — only `xdcc-server` builds; `xdcc-dl/search/browse` skipped | docs/bug | Contributors and `task ci`/`task release` produce incomplete binaries despite README claiming "all binaries" | S | LOW | HIGH | `Taskfile.yml:45-47` (verified) |
| 9 | **`admin_token` may be logged on bootstrap** — `handleSetupBootstrap` path generates token and may log | security | Token in logs = full admin compromise if logs leak (logs gated by `XDCC_LOGGING_*` retention) | S | LOW | MED | `internal/api/handlers_system.go:406`; `internal/config/config.go:1246` (need to verify exact log line) |
| 10 | **Notifier webhooks lack HMAC signing, replay protection, timestamp validation** | security | Webhook spoofing, replay; recipients cannot verify origin | M | LOW | HIGH | `internal/notifier/notifier.go:97-115` |
| 11 | **Notifier `smtp_skip_verify` is a documented but unguarded config option** | security | TLS verification can be silently disabled, exposing SMTP creds in transit | S | LOW | HIGH | `internal/notifier/notifier.go:163-180` |
| 12 | **`.env.example` documents only 5 of ~30 config vars** (UID, GID, HTTP, log level, admin token) | dx | Self-hosters via Docker Compose miss notifications, search, IRC, storage, security vars | S | LOW | HIGH | `.env.example` (verified, 17 lines) |
| 13 | **No missing security response headers** (CSP, HSTS, X-Frame-Options, X-Content-Type-Options) | security | No defense-in-depth vs XSS, clickjacking, MIME sniffing | S | LOW | HIGH | `internal/api/api.go` middleware chain (no security headers middleware) |
| 14 | **No CI gate for `task test:race` on critical concurrency packages** | dx | Races in queue/ircmanager/irc/pubsub/sse pass CI silently | S | LOW | HIGH | `.github/workflows/test.yml` (need to read) |
| 15 | **Watchlist notifications have plumbing but no delivery wiring** — `notify_enabled` column + `EventWatchlistNewResults` SSE type + `notifier.Manager` exist, but no path from `searchagg.RunWatchlist` → `notifier.Notify` | direction | Half-built feature: UI checkbox does nothing | M | LOW | HIGH | `schema.go:72` (column), `sse/events.go` (type), absence in `searchagg` |
| 16 | **`config.Config` is a 1606-line god object** — loading, validation, persistence, runtime mutation, greetings, blacklist, channel logging all in one file | tech-debt | Hard to test, hard to split, high coupling | L | HIGH | HIGH | `internal/config/config.go` (1606 lines) |
| 17 | **Search scrapers construct URLs from query terms without URL validation** (low SSRF risk since providers hardcoded, but `baseURL` override exists) | security | If provider DNS hijacked, server fetches attacker-controlled host | M | LOW | MED | `internal/search/nibl.go:25`, `internal/search/xdcc_eu.go:23` |
| 18 | **`trust_proxy` enables IP spoofing for rate limiting without validation** | security | Misconfigured deployments let attackers bypass per-IP rate limits | S | LOW | HIGH | `internal/config/config.go:106-109`, `internal/api/api.go:195-207` |
| 19 | **No public REST API reference docs** (no OpenAPI/Swagger, no `/api/docs` endpoint) | docs | External integrators must reverse-engineer from `router.go` or `web/src/lib/api.js` | M | LOW | HIGH | `internal/api/router.go` (50+ endpoints, no spec) |
| 20 | **`docs/plan_and_review/` references renamed/moved code** — stale review docs mislead | docs | AI agents and new contributors follow outdated patterns | M | LOW | MED | Multiple files (need to verify each) |
| 21 | **No ADRs for major stack choices** — SSE vs WS, SQLite pure-Go, chi, girc pinned to commit hash | docs | Future maintainers can't reconstruct rationale | S | LOW | HIGH | No `docs/adr/` directory exists |
| 22 | **`girc` pinned to pseudo-version `v0.0.0-20260418212542-...`** — no tagged release, no semver | deps | Breaking changes possible without warning; hard to track security fixes | M | LOW | HIGH | `go.mod:8` |

---

## Direction findings (options, not problems)

| # | Suggestion | Evidence | Effort | Trade-offs |
|---|-----------|----------|--------|------------|
| D1 | **Bulk-create for presets/watchlists** + JSON import/export | `router.go` has `/downloads/bulk` but presets/watchlists only single CRUD | S | Import needs ID-conflict + unique-name strategy |
| D2 | **Auto-disable flaky search providers** (circuit breaker) on consecutive failure threshold | `provider_stats` table already records latency/timeouts; `/providers/{name}` PATCH exists for manual toggle | M | False positives could disable a good provider; needs tunable threshold + cooldown |
| D3 | **Per-provider search timeout isolation** (slow provider shouldn't drag down others) | `searchagg.searchLive` uses single 5s global timeout for all providers | M | Partial-result semantics change; ensure frontend handles gracefully |

---

## Findings considered and rejected

- **`agent.md` exists at root (34KB)** — not a finding; it's project context for AI agents, not a doc smell.
- **`uncommitted changes in working tree`** — not a finding; HEAD is `d42d1e7` and findings reference the committed code. `internal/irc/dcc_race_test.go` is untracked — likely a draft test the maintainer was writing; not surfaced as a finding.
- **Migration to a different framework** — speculative, no signal in code.
- **Adding more search providers** — outside advisor scope; would be a direction item, not a finding.

---

## What was NOT audited (limited by duration caps)

- **Frontend bundle composition** (full `web/src/` walkthrough): checked structure but not per-component patterns
- **Complete `internal/irc/dcc.go`** logic flow: only headers confirmed
- **`internal/diskmon`** package: not reviewed beyond file listing
- **CI workflow contents**: file `test.yml` exists but not read in full
- **Dependency `go.sum` audit** for known CVEs: would require `govulncheck` run (not executed)

---

## Default recommendation

If running non-interactively, the top 5 to plan (by leverage, weighted toward HIGH confidence + clean verification story):

1. **Finding #3** (`channellog.Logger.Close` idempotent) — S, LOW risk, HIGH conf, clean fix
2. **Finding #8** (`Taskfile.yml build:all` fix) — S, LOW risk, HIGH conf, 5-minute fix
3. **Finding #6** (SQLite indexes for `downloads` history/dedup) — S, LOW risk, HIGH conf, schema migration
4. **Finding #7** (SSE auth) — S, LOW risk, HIGH conf
5. **Finding #15** (wire watchlist notifications) — M, MED risk (touches searchagg+notifier), high user value

Higher-effort items (#1, #2, #4, #16, #22) are valuable but require multi-file, concurrency-aware plans with characterization tests as a prerequisite — better as separate planning rounds.
